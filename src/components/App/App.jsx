import React from 'react';
import { IonApp } from '@ionic/react';

import Router from '../Router';

const App = () => (
  <IonApp>
    <Router />
  </IonApp>
);

App.propTypes = {};

export default App;
